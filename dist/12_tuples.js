"use strict";
// fixed length & fixed types
const entry = ['A', 34]; // first elel must be string then next is num and must contain no of elems
const r = [200];
const corners = [0, 0]; // readonly tuples
